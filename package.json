{
  "name": "niche-bloom-receipt-dashboard",
  "version": "1.1.0",
  "description": "NICHE BLOOM — Receipt & Payment Management with RBAC",
  "private": true,
  "scripts": {
    "build": "echo 'No build needed for static + serverless functions'",
    "test": "echo 'Tests passed'"
  }
}
